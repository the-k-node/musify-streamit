<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.musicplayerproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MusicPlayerActivity"></activity>
        <activity
            android:name=".ShreyaProfileActivity"
            android:parentActivityName=".CreditsActivity" />
        <activity
            android:name=".SidharthaProfileActivity"
            android:parentActivityName=".CreditsActivity" />
        <activity
            android:name=".KiranProfileActivity"
            android:parentActivityName=".CreditsActivity" />
        <activity
            android:name=".CreditsActivity"
            android:parentActivityName=".AccountActivity" />
        <activity android:name=".StartScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SearchActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".AccountActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>